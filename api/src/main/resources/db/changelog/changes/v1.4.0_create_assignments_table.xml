<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="v1.4.0-create-assignments-table" author="imes-dev">
        <comment>Create assignments table for Task 3.1</comment>
        
        <createTable tableName="assignments">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            
            <column name="title" type="VARCHAR(500)">
                <constraints nullable="false"/>
            </column>
            
            <column name="description" type="TEXT"/>
            
            <column name="deadline" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            
            <column name="created_by" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            
            <column name="is_active" type="BOOLEAN" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
            
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            
            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Add foreign key constraint -->
        <addForeignKeyConstraint
                baseTableName="assignments"
                baseColumnNames="created_by"
                constraintName="fk_assignments_created_by"
                referencedTableName="users"
                referencedColumnNames="id"
                onDelete="CASCADE"/>

        <!-- Add indexes for performance -->
        <createIndex indexName="idx_assignments_created_by" tableName="assignments">
            <column name="created_by"/>
        </createIndex>

        <createIndex indexName="idx_assignments_deadline" tableName="assignments">
            <column name="deadline"/>
        </createIndex>

        <createIndex indexName="idx_assignments_is_active" tableName="assignments">
            <column name="is_active"/>
        </createIndex>

        <createIndex indexName="idx_assignments_created_at" tableName="assignments">
            <column name="created_at"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
